{
  "name": "backend",
  "version": "1.0.0",
  "description": "Physics AI Backend",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "nodemon --watch . --watch ../.env --watch uploads server.js",
    "dev": "nodemon --watch . --watch ../.env --watch uploads server.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
